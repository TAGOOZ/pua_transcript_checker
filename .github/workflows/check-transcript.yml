name: Check PUA Transcript

on:
  schedule:
    # Run every hour
    - cron: '0 * * * *'
  workflow_dispatch:  # Allows manual trigger

jobs:
  check-transcript:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run transcript checker
        env:
          PUA_USERNAME: ${{ secrets.PUA_USERNAME }}
          PUA_PASSWORD: ${{ secrets.PUA_PASSWORD }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
          TELEGRAM_TARGET_USER: ${{ secrets.TELEGRAM_TARGET_USER }}
          ENABLE_TELEGRAM_RING: ${{ secrets.ENABLE_TELEGRAM_RING }}
          RING_DURATION: ${{ secrets.RING_DURATION }}
        run: python pua_transcript_checker.py
